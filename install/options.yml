hash_ip_mask: 'Should the IP mask be hashed for unauthorized board managers?'
disable_usermade_boards: 'Restrict board creation to global managers only?'
reserve_single_char_boards: 'Prevent users from claiming single character boards (like /a/ and /x/)?'
require_valid_email_owner: 'Require a validated email for a user to create or own a board?'
require_valid_email_manager: 'Require a validated email for a user to assigned as a board manager?'
external_cdn: 'Do you have or are you going to setup an external CDN?'
multiboard_ownership: 'Can a single user own more than one board at a time?'